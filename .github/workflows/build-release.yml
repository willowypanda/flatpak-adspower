name: Build and publish AdsPower Flatpak

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

jobs:
  build-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow startup test
        run: echo "GitHub Actions workflow started successfully"
