# GitHub Pages placeholder

This directory is populated automatically by the GitHub Actions workflow. After a successful release, `docs/index.html` and `docs/com.adspower.global.flatpakref` will contain the latest bundle download link.

Enable GitHub Pages in repository settings and select the `docs/` folder as the source.
